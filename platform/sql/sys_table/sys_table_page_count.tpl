-- Code generated by dol build. Only Generate by tools if not existed.
select
    count(*) records
from
	sys_table
where
	sys_table.id {{.ne}} ""
	and
	sys_table.del_flag {{.ne}} 1
{{if ne .role_rule ""}}
	and {{.role_rule}}
{{end}}
