apiVersion: v1
kind: Ingress
metadata:
  name: ami
  namespace: dolphin
spec:
  rules:
  - host: www.yourhost.com
    http:
      paths:
      - backend:
          serviceName: ami
          servicePort: 80
        path: /
  tls:
  - hosts:
    - www.yourhost.com
    secretName: www-tls
