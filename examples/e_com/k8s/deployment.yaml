apiVersion: v1
kind: Deployment
metadata:
  name: e_com
  namespace: dolphin
spec:
  selector:
    matchLabels:
      app: e_com
  template:
    metadata:
      labels:
        app: e_com
    spec:
      containers:
      - name: e_com
        image: registry.cn-hangzhou.aliyuncs.com/e_com:1001
        volumeMounts:
          - name: config
            mountPath: /app/app.properties
            subPath: app.properties
        resources:
          limits:
            memory: "2Gi"
            cpu: "1000m"
          requests:
            memory: "10Mi"
            cpu: "11m"
        ports:
        - containerPort: 80
      imagePullSecrets:
        - name: registrykey
      volumes:
        - name: config
          configMap:
            name: e_com