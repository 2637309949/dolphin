<!-- Code generated by dol build. Only Generate by tools if not existed. -->
<sqlMap>
    <sql id="insert_mc_follow">
        insert into mc_follow
		(`user_id`,`followable_id`,`followable_type`,`create_time`)
		values
		(?user_id,?followable_id,?followable_type,?create_time)
    </sql>
    <sql id="update_mc_follow">
        update mc_follow set `user_id`=?user_id,`followable_id`=?followable_id,`followable_type`=?followable_type,`create_time`=?create_time
		where id =?id
    </sql>
    <sql id="deleteone_mc_follow">
        delete from mc_follow where id =?id
    </sql>
    <sql id="selectone_mc_follow">
        select `user_id`,`followable_id`,`followable_type`,`create_time` from mc_follow where id =?id
    </sql>
    <sql id="selectall_mc_follow">
        select `user_id`,`followable_id`,`followable_type`,`create_time` from mc_follow
    </sql>
</sqlMap>
