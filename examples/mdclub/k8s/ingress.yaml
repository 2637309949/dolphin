apiVersion: v1
kind: Ingress
metadata:
  name: mdclub
  namespace: dolphin
spec:
  rules:
  - host: www.yourhost.com
    http:
      paths:
      - backend:
          serviceName: mdclub
          servicePort: 80
        path: /
  tls:
  - hosts:
    - www.yourhost.com
    secretName: www-tls
