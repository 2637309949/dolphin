apiVersion: v1
kind: Deployment
metadata:
  name: mdclub
  namespace: dolphin
spec:
  selector:
    matchLabels:
      app: mdclub
  template:
    metadata:
      labels:
        app: mdclub
    spec:
      containers:
      - name: mdclub
        image: registry.cn-hangzhou.aliyuncs.com/mdclub:1001
        volumeMounts:
          - name: config
            mountPath: /app/app.properties
            subPath: app.properties
        resources:
          limits:
            memory: "2Gi"
            cpu: "1000m"
          requests:
            memory: "10Mi"
            cpu: "11m"
        ports:
        - containerPort: 80
      imagePullSecrets:
        - name: registrykey
      volumes:
        - name: config
          configMap:
            name: mdclub