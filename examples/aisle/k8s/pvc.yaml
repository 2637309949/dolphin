apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aisle
  namespace: dolphin
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
