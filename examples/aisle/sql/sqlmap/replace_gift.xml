<!-- Code generated by dol build. Only Generate by tools if not existed. -->
<sqlMap>
    <sql id="insert_replace_gift">
        insert into replace_gift
		(`r_g_id`,`sg_id`,`remark`,`creater`,`create_date`,`updater`,`update_date`,`is_delete`)
		values
		(?r_g_id,?sg_id,?remark,?creater,?create_date,?updater,?update_date,?is_delete)
    </sql>
    <sql id="update_replace_gift">
        update replace_gift set `r_g_id`=?r_g_id,`sg_id`=?sg_id,`remark`=?remark,`creater`=?creater,`create_date`=?create_date,`updater`=?updater,`update_date`=?update_date,`is_delete`=?is_delete
		where id =?id
    </sql>
    <sql id="deleteone_replace_gift">
        delete from replace_gift where id =?id
    </sql>
    <sql id="selectone_replace_gift">
        select `r_g_id`,`sg_id`,`remark`,`creater`,`create_date`,`updater`,`update_date`,`is_delete` from replace_gift where id =?id
    </sql>
    <sql id="selectall_replace_gift">
        select `r_g_id`,`sg_id`,`remark`,`creater`,`create_date`,`updater`,`update_date`,`is_delete` from replace_gift
    </sql>
</sqlMap>
