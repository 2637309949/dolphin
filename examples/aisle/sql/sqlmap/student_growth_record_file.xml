<!-- Code generated by dol build. Only Generate by tools if not existed. -->
<sqlMap>
    <sql id="insert_student_growth_record_file">
        insert into student_growth_record_file
		(`s_g_r_f_id`,`sgr_id`,`file`,`creater`,`create_date`,`updater`,`update_date`,`isdelete`)
		values
		(?s_g_r_f_id,?sgr_id,?file,?creater,?create_date,?updater,?update_date,?isdelete)
    </sql>
    <sql id="update_student_growth_record_file">
        update student_growth_record_file set `s_g_r_f_id`=?s_g_r_f_id,`sgr_id`=?sgr_id,`file`=?file,`creater`=?creater,`create_date`=?create_date,`updater`=?updater,`update_date`=?update_date,`isdelete`=?isdelete
		where  id =?id
    </sql>
    <sql id="delete_student_growth_record_file">
        delete from student_growth_record_file
		where id =?id
    </sql>
    <sql id="selectone_student_growth_record_file">
        select `s_g_r_f_id`,`sgr_id`,`file`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from student_growth_record_file
		where  id =?id
    </sql>
    <sql id="selectall_student_growth_record_file">
        select `s_g_r_f_id`,`sgr_id`,`file`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from student_growth_record_file
    </sql>
</sqlMap>
