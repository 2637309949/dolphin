apiVersion: v1
kind: Ingress
metadata:
  name: hello
  namespace: dolphin
spec:
  rules:
  - host: www.yourhost.com
    http:
      paths:
      - backend:
          serviceName: hello
          servicePort: 80
        path: /
  tls:
  - hosts:
    - www.yourhost.com
    secretName: www-tls
