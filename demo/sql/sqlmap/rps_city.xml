<!-- Code generated by dol build. Only Generate by tools if not existed. -->
<sqlMap>
    <sql id="insert_rps_city">
        insert into rps_city
		(`rps_city_id`,`rps_id`,`organ_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete`)
		values
		(?rps_city_id,?rps_id,?organ_id,?creater,?create_date,?updater,?update_date,?isdelete)
    </sql>
    <sql id="update_rps_city">
        update rps_city set `rps_city_id`=?rps_city_id,`rps_id`=?rps_id,`organ_id`=?organ_id,`creater`=?creater,`create_date`=?create_date,`updater`=?updater,`update_date`=?update_date,`isdelete`=?isdelete
		where  id =?id
    </sql>
    <sql id="delete_rps_city">
        delete from rps_city
		where id =?id
    </sql>
    <sql id="selectone_rps_city">
        select `rps_city_id`,`rps_id`,`organ_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from rps_city
		where  id =?id
    </sql>
    <sql id="selectall_rps_city">
        select `rps_city_id`,`rps_id`,`organ_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from rps_city
    </sql>
</sqlMap>
