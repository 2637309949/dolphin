<!-- Code generated by dol build. Only Generate by tools if not existed. -->
<sqlMap>
    <sql id="insert_gai_file">
        insert into gai_file
		(`gai_file_id`,`gai_id`,`file_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete`)
		values
		(?gai_file_id,?gai_id,?file_id,?creater,?create_date,?updater,?update_date,?isdelete)
    </sql>
    <sql id="update_gai_file">
        update gai_file set `gai_file_id`=?gai_file_id,`gai_id`=?gai_id,`file_id`=?file_id,`creater`=?creater,`create_date`=?create_date,`updater`=?updater,`update_date`=?update_date,`isdelete`=?isdelete
		where  id =?id
    </sql>
    <sql id="delete_gai_file">
        delete from gai_file
		where id =?id
    </sql>
    <sql id="selectone_gai_file">
        select `gai_file_id`,`gai_id`,`file_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from gai_file
		where  id =?id
    </sql>
    <sql id="selectall_gai_file">
        select `gai_file_id`,`gai_id`,`file_id`,`creater`,`create_date`,`updater`,`update_date`,`isdelete` from gai_file
    </sql>
</sqlMap>
