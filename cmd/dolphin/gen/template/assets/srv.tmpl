// Code generated by dol build. Only Generate by tools if not existed.
// source: {{.Controller.Name}}.go

package srv

import (
	"context"
	"errors"
	"time"

	"github.com/gin-gonic/gin"
	"github.com/sirupsen/logrus"
	"github.com/2637309949/dolphin/packages/xormplus/xorm"
)

// {{$.Controller.ToUpperCase $.Controller.Name}}TODO defined srv
func {{$.Controller.ToUpperCase $.Controller.Name}}TODO(ginCtx *gin.Context, db *xorm.Engine, actCtx context.Context, params struct{}) (interface{}, error) {
	actCtx, cancel := context.WithTimeout(actCtx, 5*time.Second)
	go func(ctx context.Context) {
		ticker := time.NewTicker(1 * time.Second)
		for range ticker.C {
			select {
			case {{.lt}}-ctx.Done():
				logrus.Infoln("child process interrupt...")
				return
			default:
				logrus.Infoln("child job...")
			}
		}
	}(actCtx)
	defer cancel()
	{{.lt}}-actCtx.Done()
	time.Sleep(1 * time.Second)
	logrus.Infoln("main process exit!")
	return nil, errors.New("no implementation found")
}